# jule-playground

> An online sandbox to quickly test Jule code.

## Setup

Since `playground.js` uses ES modules, you need to run a static server.

```bash
python3 -m http.server
```

Then open `localhost:8080` in your browser.
